on:
  pull_request:
    branches:
      - main
jobs:
  name: Compile and Test
  steps:
    - uses: actions/checkout@v2
    - uses: actions/setup-node@v2
      with:
        node-version: "14"
    - run: npm install
    - run: npm test
    - run: npm build
