// Strategy: Go through the array of issues attached to each student.
// Look at each one and determine which are a problem and which are
// not a problem. If there is a validator associated with the issue,
// run it; also do basic sanity checks (e.g., if the weight is 0,
// ignore).
const tagValidators = {};

export default tagValidators;
