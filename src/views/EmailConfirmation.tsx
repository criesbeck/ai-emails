import React from "react";
import { Flex } from "@chakra-ui/react";

const EmailConfirmation: React.FC = () => {
  return (
    <Flex data-testid="confirm-emails-page">
      <h1>Hello!</h1>
    </Flex>
  );
};

export default EmailConfirmation;
